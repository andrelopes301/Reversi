<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isec.kotlin.reversi">

    <!-- Permissoes para usar a camera -->
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_reversi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reversi">

        <activity
            android:name=".atividades.FotografiaActivity"
            android:configChanges="orientation"
            android:exported="true" />
        <activity
            android:name=".atividades.PerfilActivity"
            android:configChanges="orientation"
            android:exported="true" />
        <activity
            android:name=".atividades.JogoActivity"
            android:configChanges="orientation"
            android:exported="true" />
        <activity
            android:name=".atividades.ModoJogoActivity"
            android:configChanges="orientation"
            android:exported="true" />
        <activity
            android:name=".atividades.OpcoesActivity"
            android:configChanges="orientation"
            android:exported="true" />
        <activity
            android:name=".atividades.ScoresActivity"
            android:configChanges="orientation"
            android:exported="true"/>
        <activity
            android:name=".atividades.MainActivity"
            android:configChanges="orientation"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>